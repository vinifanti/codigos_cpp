/*

5.22-) Analisar o trecho de código abaixo e justificar porque o valor da função y não é calculado.

        if (2 > 1)
            cout << "y = sqrt(x)";

Resposta: o valor da função y não é calculado pois a expressão que calcula está entre "" o que faz com que o compilador
reconheça como uma string e não como uma expressão matemática.
*/
