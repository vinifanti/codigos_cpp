#include <iostream>
#include <cmath>
using namespace std;

int main(){
    double y, x;
    cout << "x: "; cin >> x;
    y = sqrt((pow(x, 2) + 3.0) / 2.0);
    cout << "y: " << y << endl;
return 0;
system("pause");
}